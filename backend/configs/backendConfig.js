'use strict';

const BackendConfig = {
    FPS: 10,
    MAP_SIZE: 60,
    NUMBER_OF_FIELDS: { //the number of fields to be generated on the map
        APPLE: 20,
        OBSTACLE: 30,
    }
}

module.exports = BackendConfig;